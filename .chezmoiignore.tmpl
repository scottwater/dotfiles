# Zed auto-managed
.config/zed/prompts/**
.config/zed/conversations/**
.config/zed/backup/**
.config/zed/themes/**

# LazyVim auto-managed
.config/nvim/lazy-lock.json
.config/nvim/lazyvim.json
.config/nvim/.neoconf.json

# LazyVim spelling dictionaries (ignored for now)
.config/nvim/spell/**

# macOS
**/.DS_Store

# Homebrew is macOS-only now (mise handles CLI tools on Linux)
{{- if ne .chezmoi.os "darwin" }}
Brewfile
{{- end }}

# Work-only files
{{- if not (get . "kode") }}
.agents/skills/linear/**
.agents/skills/bugsnag/**
{{- end }}

#backup
backup/

# Project-specific files (not for syncing)
CLAUDE.md
AGENTS.md
