# Bat configuration
# ANSI delegates syntax colors to Ghostty's Tokyo Night Dark palette.
--theme="ansi"

# Show line numbers
--style="numbers,changes,header,grid"

# Use italic text (works in most modern terminals)
--italic-text=always

# Set tab width
--tabs=4

# Enable paging for long output
--paging=auto

# Map additional file types
--map-syntax "*.conf:INI"
--map-syntax ".gitconfig:INI"
--map-syntax "*.envrc:Bash"
--map-syntax ".envrc:Bash"
